{
  "name": "@openclaw/reef-relay",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json --noEmit && node scripts/build-docs.mjs",
    "dev": "wrangler dev --var DEV_MODE:1",
    "test": "node --test scripts/*.node-test.mjs && vitest run",
    "types": "wrangler types --include-runtime false --strict-vars false"
  },
  "dependencies": {
    "@openclaw/reef-protocol": "workspace:*"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "0.22.0",
    "@cloudflare/workers-types": "5.20260904.1",
    "@noble/curves": "2.4.0",
    "marked": "18.0.11",
    "wrangler": "4.129.0"
  }
}
