apiVersion: v1
kind: Service
metadata:
  name: {{ include "reef-relay.fullname" . }}
  labels:
    {{- include "reef-relay.labels" . | nindent 4 }}
spec:
  selector:
    {{- include "reef-relay.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
