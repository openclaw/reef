{
  "name": "reef",
  "private": true,
  "packageManager": "pnpm@10.32.1",
  "engines": {
    "node": ">=20.19.0"
  },
  "scripts": {
    "build": "pnpm -r build",
    "e2e": "tsc -p e2e/tsconfig.json --noEmit && vitest run --config e2e/vitest.config.ts",
    "test": "pnpm -r test"
  },
  "devDependencies": {
    "@types/node": "26.1.1",
    "typescript": "7.0.2",
    "vitest": "4.1.10"
  }
}
